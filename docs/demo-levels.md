# First-party demo levels — the mechanic showcase backlog

**Every new mechanic gets a small first-party level that verifies and shows it
off.** Not necessarily right when the mechanic lands — but it must be queued
here, and this queue is the planning
agent's **standing idle work**: no urgent development task + owner not
responding → build the next level in this list (full ladder green, content-repo
PR, playable via the play profile).

Rules of the queue:

- A demo level is SMALL: one mechanic in the spotlight, 10–20 minutes, minimum
  cast. It doubles as living documentation — a stranger (or a future /new-delve
  session) plays it and understands the mechanic's intended use.
- "Covered by" means a shipped full campaign already showcases the mechanic
  prominently; no separate demo needed unless the coverage rots.
- Levels are authored via /new-delve like any campaign (dogfooding — friction
  found here is toolchain work, which is the point; polishing levels is the
  driver for toolchain/prompt improvement).
- New mechanics: the PR that lands a mechanic adds a row here (same-PR rule,
  like docs/reference sync).

## Mechanic demos

| Mechanic (spec) | Demo concept | Status |
|---|---|---|
| Traps: stair-volley / dart gallery / trapped chest (0011) | **The Toll Road** — a short fortified pass where every trap type guards one alcove of loot; disarm levers teach counterplay | pending |
| Checkpoints + stealth zones (0012) | covered by nobodys-cave-island (blind-giant climb) | covered |
| Actors, staging, cutscenes (0014) | **The Wake** — a funeral procession level that is 80% staging: mourners walk, a eulogy sequence, one player choice redirects the procession | pending |
| Bonfire rest + wave re-seat (0016 §1) | covered by the-drowned-bell | covered |
| Shortcut loop-back (0016 §2) | covered by the-drowned-bell (chapel door) | covered |
| Ambush (0016 §3) | covered by the-drowned-bell (wall-watch, rafters) | covered |
| Timed gate (0016 §4) | **Tide Mill** — a mill race where the water gate cycles; the whole level is timing runs through 3 gates, escalating windows | pending |
| TD lanes + aggro-edge (0016 §6) | **Hold the Causeway** — pure defense: three waves down two lanes at a barricade the party pre-walks | pending |
| Party division of labor (0018) | **Two Keys** — a 2-player vault where progression is provably split (carrier:one items, AND-join objectives); solo-completable per DW0356 floor | pending |
| Point of no return: close-gate + inter-area transport | covered by nobodys-cave-island (the boulder) | covered |
| strike-npc + vanilla-warden combat consequence | covered by nobodys-cave-island (round 11) | covered |
| Bark pools + scene ledger (0020, landing) | **Market Day** — a town-square level dense with background NPCs; one real quest threads through a crowd of bark-pool characters; the cast ledger is the star | pending (blocked on 0020) |
| Cutscene rehearsal/calibration (0019, landing) | tooling, not a level — its demo is a GENERATION.md walkthrough of calibrating one shot | pending (blocked on 0019) |
| Review cameras stand in open air (`DW0724`, 0003 visual tier) | tooling, not a level — its demo is a pair of `delvec snapshot --camera` frames of one shot: the requested stand-off renders a single flat colour, the stood-up camera renders the room. The plan names both positions on the shot, so the pair is reproducible from the artifact | pending |
| Grammar arguments — one rule called with different content (`bind`, 0033 §4.6) | generation-time, so its demo is a corpus program rather than a level: `delve-grammar expand --program idiom-arguments --region 15x7x15 --seed 1` builds four pointed-arch heads, two paints and two axes, out of one recursion | covered (`idiom-arguments`) |
| i18n zh-cn sidecar | covered by both shipped campaigns | covered |
| Map-editor terrain pass (0017) | covered by nobodys-cave-island (de-walling, beach seam) | covered |
| Instanced claims + exterior transit edges (0041) | generation-time, so its demo is a corpus pair rather than a level: one colonnade rule called twice under `qualify` — east run a walked space, west run out-of-walk scenery — and one recursion-built stair climbing out of the piece as an exterior `stair` edge whose face contract carries the exit; both expand green and the twins that misdeclare them are the spec's red fixtures | pending (blocked on 0041) |
| Grammar frame reflection + `symmetric` gate (grammar.md §2, §4c) | **The Mirrored Hall** — a small symmetric hall whose every paired feature (twin transept arms, twin stairs, a rose window) is one rule and its reflection; the level's README shows the program, and the build gates on `--symmetric x` so a forgotten reflection is a red rather than a hole in a flank | pending |
| Ocean horizon / pseudo-open-world boundary (0013) | covered by nobodys-cave-island | covered |
| Spatial contract — declared spaces checked against the blocks (0036, ADR-0020) | **The Two Wards** — two pieces that each pass every gate alone and refuse to assemble: ward A declares a gate on its east face, ward B declares its own a course higher, and `delvec` refuses the pair by name (`DW0780`) before anything is built. The level's README then shows the same two pieces corrected, and walks the contract of one of them — an `enclosed` nave, an `open_top` yard, a `barred` shortcut, a `posted` gallery — beside the verdict block that names each opt-out | pending |
| What the world settles bytes into — stair `shape` and a body of fluid (`DW0801`, `DW0800`) | **The Mill Race** — one small water-mill yard, built twice and shown side by side: the version a render approves, and the version the server keeps. The first has a mitred kerb pointed across its run and a channel one course short of its wall; the second is the same yard one property and one course apart. The player walks the second, and the level's README carries the half of the comparison that can be photographed — the kept yard's kerb cells, which are block for block what the drawn yard settles to after one update — beside the refusal transcripts for the half that cannot. There is no picture of the drawn yard in either state: `expand` judges before it freezes, so red bytes are never written, and the settled water is mid-flow water, which a piece may not pin. The rules are therefore demonstrated by the two reports naming their cells, and the absent picture is the demonstration, not a gap in it | built (content repo, `demos/mill-race`) |
| Attachable supports — where a vine or a lichen may hold on (`connections::attachable_faces`) | **The Dripstone Gallery** — one small cavern room, one seeded decoration pass, and four bays a player walks in a line. Bay 1: rock on one wall only, so the growth is on that wall and on the face that *touches* it. Bay 2: no wall within reach and a solid ceiling, so the same pass hangs the growth overhead instead of dropping it — the bay that used to come out bare. Bay 3: nothing but dripstone tips and a hanging lantern, so the pass places nothing and the bay is honestly empty, because a tip cannot hold a decal and a face that names one is deleted at the first block update. Bay 4: a vine and a lichen in the same cell shape, showing the vine offered five faces and the lichen six. Its README puts the emitted states beside the picture, so the level is the claim | pending |
| Block palette selection — screened shelf + mix report (0035) | **Two Naves** — one grammar program, one region, one seed, expanded twice under two palettes whose mean colours sit 13.5 RGB units apart and whose chromatic areas are 60% and 30%. The player walks from one into the other; the point is that a number said they were the same room and the eye says they are different buildings. Ships with its swatch sheet and both mix reports beside it, so the level IS the argument for measuring area share instead of a mean | pending |
| Author-aimed review camera — a bearing plus a subject box (`delve-render --view`) | **The Gatehouse Elevations** — one small gatehouse piece, two review sets of the same bytes, shown side by side. The derived set is everything the renderer plans on its own: four corner isometrics, a plan, and an oblique per socket and per anchor. Not one of them photographs a FACE square-on, so the west front, the flank and the arch a party walks under have no picture in their own review set, and a reviewer asked "is this a gatehouse" is answering about the instrument. The authored set is the same piece with three views — the front (`face=north of=model`), the flank (`face=west of=model`) and the arch (`face=north of=anchor/gate`) — each framed to that face at the standoff the face asks for, not at a `zoom=` rediscovered per building; the first two share a subject and differ only in bearing, the first and third share a bearing and differ only in subject, which is the whole surface. Its second half is the refusals, because a set that silently drops the one camera the reviewer asked for still looks complete in a directory listing: a subject the piece does not declare (the message lists the ones it does), a bearing given twice or not at all, and a name a planned shot already holds are each `DW0721` before a single frame renders, and a view that frames empty air is `DW0727` repeating the bearing and zoom that produced it rather than a blank PNG nobody scrolls back to | built (content repo, `demos/gatehouse-elevations`) |
| Why a rule did not apply — guard exhaustion names every failing comparison, both operands, and the derivation path (grammar.md §4) | generation-time, so its demo is a corpus program rather than a level: one program whose plan rule sits behind five nested comparisons, two splits and a reorientation, expanded at the region one block under the width its own guard requires. The artifact IS the refusal. It reports every failed conjunct of the `all` at once — not the first — plus the conditions held under `none_of`, required-vs-actual orientation, and the conjuncts it could not evaluate by name, each with its authored expression beside its value at that scope; then the scope's local dims, orientation and world box, and the rule names and split pieces that produced it. The argument the program makes is that the dimensions at the failure site are NOT the region on the command line — reorientation and a chain of splits have moved them — so a refusal naming only the rule leaves an author sweeping x/y/z by brute force. The walkthrough checks the printed arithmetic instead of trusting it: re-run the same program one block larger and one block smaller and the pass/fail boundary lands exactly where the message said it would | built (content repo, `demos/guard-exhaustion`) |
| Document-level composition — one program file composes another (`include`, 0040 §6.2) | generation-time, so its demo is a corpus program rather than a level: a map document that composes two zone documents into one derivation, expanded at the region its splits allocate. It shows the arrangement 0040 §4 requires — the map writes what neither zone can know (the boxes they get, the mass between them) and pushes its one whole-map datum down by `bind` — and that the composition is byte-for-byte the Rust composition it replaces. Its second half is the bound 0040 §1.4 measured and §5 draws the cost of, which is the part a reader will otherwise assume away: the same piece composed under a host that draws first re-textures, so a composed piece's look is certified by the composed render and never by its own | pending |
| Local axis frame on a paint (grammar) | **The Turning Gate** — one grammar program, one barred doorway, built four times into boxes turned against each other; the same palette role gives the right bars, stair and skull yaw in every one, and the level's own signage is the before/after. Its second half is the refusal: a scope tipped onto its side, where the yaw has no image and the build says so instead of guessing | pending |
| The third gate answer — a region that cannot decide `oriented-fills` (`DW0742`) | generation-time, so its demo is a corpus program rather than a level: **one** program with one run of world-frame bars under a `z: largest` root, expanded at a box and at its transpose and shown as a pair. At the first the gate says `undecided` and names the fill, the property and the request; at the second the SAME gate refuses the SAME fill by name under `DW0736`. The artifact is the pair, because either half alone is the thing the verdict exists to stop being read on its own — the first looks like a piece with a note attached, the second like a piece with a bug, and only together do they show that the note WAS the bug, a quarter-turn away. The walkthrough then wraps the state as `{"local": …}` and re-runs both regions: `pass` at each, one binding, and the local-frame count says where the population went | pending |
| A zone past the vanilla 48-per-axis cap, placed whole (chunked export phase 2) | **The Long Gallery** — one hall a player walks end to end that is physically impossible to ship in one structure template: 60 blocks of colonnade, cut at 48 by the exporter and reassembled by the compiler. The mechanic is that there is nothing to see. The campaign binds `prefab/long-gallery` in the one line any prefab is bound in; the far end of the hall, where the level's only objective sits, is in the second tile; and the seam is a course of the same floor as everything either side of it. Its README puts the three artifacts beside the walk — the manifest, the two `.nbt`s, and the emitted `place_all` with its two lines 48 apart — so a reader can see that the author's document mentions neither. Its second half is the failure that made the mechanic worth a demo: the same gallery with one tile left at an older export, which is `DW0803` before the world is built, because every pass but the placement reads the size the manifest declares and only the blocks know the other number | pending |
| A way that content opens — contingent edges, both signs, and `open-way` (0042) | **The Mended Span** — two rooms and a belvedere, severed twice: a footbridge whose deck is missing (a `walk` with a laid way) and a broken flight (a `stair` with one), plus one portcullis the same surface states as a cleared way on a climb. The party finds the planks and the crank in the first room; two `open-way` effects lay the deck and clear the grate, and the compile verdict is part of the exhibit — the seam line naming each space "reached only once … is laid", and the disposition line carrying one deliberately never-opened way with nothing staged behind it, printed and green. The walkthrough crosses both openings with the bot (AC13), and its second half is the red pair: the same campaign with the opening effect moved after the objective, and the same piece with the treads shipped in bytes under a declared way, each refused by name | pending |
| Fluid against the horizon — placed water is judged where it lands (`DW0318`) | **The Cistern and the Cliff** — one small level built twice from the same pieces, and the level IS the pair. A cistern room whose water is walled by its own bytes, and beside it a shelf whose pond runs to the open edge; under `horizon: ocean` the shelf is a shore and the build is green, and under `horizon: void` the identical geometry is refused, because the water it was pouring into the sea is now pouring off the edge of the world forever. The point a reader should leave with is that neither piece changed — the ambient did — which is why this obligation cannot live in a prefab and cannot be answered by a declaration on a face. Its second half is what the refusal is worth: the void build is walked with the check disabled to show what shipped before it existed (a waterfall with no bottom, drawn by nothing, and the boundary proof silent because falling into water counts as a landing), then re-walked with it on. The ledger is shown open at both ends: `validation/fluid-escape.json` states the fluid cells and pieces it examined even on the cistern-only build, where the count binds to a pond nobody was worried about | pending |
| The numbers a level is built to — the metrics standard and its calibration walk (`delvec metrics`, 0049 §2.3) | **The Metrics Gym** — one level generated FROM the table, so it cannot drift from what it documents: a corridor at width 1, 2 and 3 side by side; every standard seam opening and the openings one cell either side of it; each stair pitch and each storey height as a bay a player climbs; one room per size class built at both of its bounds. Nothing in it is authored geometry — it is an ordinary site plan run through the ordinary derivation, so the walk that calibrates the standard and the first exercise of the whole slice are the same hour. What the walker rules edits the table and flips `calibrated`, one entry at a time, and the gym regenerates as the standard's living documentation. Until then every build says so: the table lands with all of it uncalibrated and `DW0813` names, per run, the seeds each verdict rested on. Its README puts the two halves beside each other — the player metrics as facts nobody may vote on, and the building metrics as choices this walk exists to make — because the level's argument is that the difference is real and that only one half is walkable | pending (blocked on 0049 round 5: the gym's generator. The derivation it is built by landed in round 4) |
| A whole map with no authored geometry — the derived blockout and its observer (0049 §5) | **The Derived Whole** — five places, four small JSON documents and not one line that describes a block. The level is walked to answer one question a picture cannot: does a map you never built read as a map? A body spawns on a floor whose colour names the place it is standing in, walks a seam the plan cut, climbs a slab ramp the derivation chose the pitch of, is stopped by a barred way until the keeper yields, and falls through a hole that closes the loop back to the hub. The exhibit is that a plan edit and a regeneration are the same act: the walkthrough moves one box eight blocks and rebuilds in front of the reader, and the whole map moves with it — no hand edit to lose, because there was never a hand edit to make. Its second half is the observer, shown as three pairs: the same plan derived once correctly and once with the derivation asked to cut every hole a cell over, to leave one place solid, and to build the walls a course tall — `DW0836`, `DW0837` and `DW0838` naming each in turn, so a reader can see that the checks are reading the blocks rather than reciting the arithmetic that laid them. The build's own two binding lines are part of the exhibit, because a green that examined nothing looks exactly like this one | pending |

## M5 theme suite (all five)

Genre-diverse levels beyond the Greek-myth and souls lines; each exercises a
distinct authoring register:

1. **Mystery** — a manor whodunit: flags-as-clues, dialogue trees as
   interrogation, the accusation is a branching finale. Zero combat; tests
   whether our dialogue/flag machinery carries a level alone.
2. **Horror** — one long stealth crescendo in a lightless mine (declared
   darkness + mitigation): sound design via play-sound, a pursuer on the
   vanilla-warden pattern, scarce checkpoints tuned merciful.
3. **Tower defense** — Hold the Causeway grown into a full night: build-phase
   walks, three sieges, NPC militia as staged actors.
4. **Heist** — timed gates + patrol routes + party split: get in, get the
   relic, get out before the watch cycles; loud fallback if caught.
5. **Pastoral / cozy** — no fail state at all: a festival day of fetch-and-talk
   beats, bark-pool crowds, one cutscene sunset. Tests whether the engine can
   be gentle.
