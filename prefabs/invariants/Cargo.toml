[package]
name = "prefab-invariants"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "prefab_invariants"
path = "src/lib.rs"

[dependencies]
# Block knowledge — the pinned registry's own crate. `blockshape` and `fluid`
# are re-exported from here rather than source-included out of `crates/`.
delvewright-dsl.workspace = true
serde.workspace = true
serde_json.workspace = true
