{
  "code": "DW0845",
  "units": [],
  "why": "The site-plan overlay's own `programs/annex.json` with its one `mark` removed, so no anchor of the expansion answers `anchor/node-annex`, the name the campaign's quest layer bound to this place before any detail existed. `delvec detail` binds an owed name to the piece anchor of the same stem and judges the row it would write before writing it, so the refusal is `DW0845` in validation's own words and no file moves. It declares no unit: what it perturbs is a program, and what must never force a quest edit is a place being detailed."
}
